<Window
  x:Class="WPFAdorner.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:WPFAdorner"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:userControls="clr-namespace:WPFAdorner.Views.UserControls"
  xmlns:viewModels="clr-namespace:WPFAdorner.ViewModels"
  xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
  xmlns:behavior="clr-namespace:KeyBoard.WPF.Behavior;assembly=KeyBoard.WPF"
  Title="MainWindow"
  Width="800"
  Height="450"
  WindowState="Maximized"
  mc:Ignorable="d">
  <Window.DataContext>
    <viewModels:MainWindowViewModel />
  </Window.DataContext>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="60" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal">
      <Button Width="60" Command="{Binding SelectImageFileCommand}">选择图片</Button>
      <Button Width="60" Command="{Binding AddRectCommand}">添加矩形</Button>
      <Button Width="60" Command="{Binding AddEllipseCommand}">添加圆形</Button>
      <Button Width="60" Command="{Binding AddFreeFormCommand}">添加自定义</Button>
      <TextBox Width="100">
        <b:Interaction.Behaviors>
          <behavior:KeyboardBehavior UCBackground="DeepPink"/>
        </b:Interaction.Behaviors>
      </TextBox>
    </StackPanel>
    <userControls:ImageUserControl Grid.Row="1" ImageSource="{Binding Bitmap}" Adorners="{Binding Adorners}" />
  </Grid>
</Window>
